Fix the keyring path
--- a/Src/mix.c
+++ b/Src/mix.c
@@ -512,7 +512,7 @@
 	RELFINAL = 99;
 	MAXLAT = 36 * 60 * 60;
 	MINLAT = 5 * 60;
-	strnncpy(PGPPUBRING, "");
+	strnncpy(PGPPUBRING, "~/.gnupg/pubring.gpg");
 	strnncpy(PGPSECRING, "");
 #ifdef COMPILEDPASS
 	strnncpy(PASSPHRASE, COMPILEDPASS);
